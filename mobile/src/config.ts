/**
 * MOCK_MODE = true  → kamera + backend gerekmez, sahte veri döngüsü çalışır
 * MOCK_MODE = false → gerçek WebSocket + kamera kullan (prod/demo)
 *
 * Değiştirmek için bu satırı düzenle, kaydet, Expo otomatik yeniler.
 */
export const MOCK_MODE = false;
