__pycache__/
*.pyc
.venv/
venv/
.env
*.sqlite3
*.db
*.pt
*.onnx
*.engine
runs/
node_modules/
.expo/
dist/
web-build/
mock/sample_frames/*.jpg
mock/*.mp4
.DS_Store
.pytest_cache/

# Sunucu runtime dosyaları
.server.pid
server.log
server.out.log

# Eğitim verisi ve geçici değerlendirme çıktıları (büyük; yeniden üretilebilir)
datasets/
eval_perclass.log
# models/*.pt ağırlıkları Git LFS ile commit'lenir (bkz .gitattributes) → *.pt ignore'undan muaf.
# (runs/ ve kök stray .pt hâlâ ignore'da kalır — yalnız models/ paylaşılır.)
!models/*.pt
# Eğitim/değerlendirme geçici logları
*_train.log
*_perclass.log
supplement.log
v1_*.log
types_*.log
qod_*.log
eval_real.log
# Geçici (commit'lenmeyen) yardımcı
eval/_perclass_tmp.py
speed_eval.log

# Claude Code yerel araçları/lock (kişisel — paylaşılmaz)
.claude/scheduled_tasks.lock
.claude/agents/
.claude/skills/
